import React, { useContext } from 'react';
import {AppContext} from "../context/appContext";

function Cart(){
    return(
        <main className="cart-page">
            <h1>Checkout</h1>
        </main>
    )
}

export default Cart